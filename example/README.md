# Dockerized Valgrind Example
`main.c` is a program that leaks an int.

## Run on macOS & linux
Run:
```
$ ./valgrind.sh --src=./example
```

## Run on Windows
Run:
```
$ ./valgrind.ps1 --Source ./example
```